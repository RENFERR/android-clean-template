package com.example.data.utils

object ApiConfig {

    const val BASE_URL = "www.example.com/api/"

    object Requests {

        object GetContent {
            const val path = "example"
            const val queryParam = "param"
        }
    }
}